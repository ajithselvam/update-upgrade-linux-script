# update-upgrade-linux-script

## 🧰 System Update & Upgrade Script

A simple Bash script to update and upgrade all APT packages safely on Debian/Ubuntu-based systems.

### 🔧 Usage

1. Clone the repository or download the script:
   ```bash
   git clone https://github.com/<your-username>/<repo-name>.git
   cd <repo-name>
2. Make the script executable:
chmod +x update-upgrade.sh
3. Run the script:
 ./update-upgrade.sh
